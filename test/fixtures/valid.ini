# Logging configuration
[Log]
# optional, use stdout if not set
path = out.log
# mandatory, one of "debug", "info", "warning", "error"
level = info
# Redis configuration
[Redis]
# mandatory, username and password are optional
url = redis://username:secretpwd@localhost:8888
# optional, timeout in sec
connectTimeout = 10.5
